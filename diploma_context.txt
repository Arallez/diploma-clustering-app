# 🎓 ПАСПОРТ ДИПЛОМНОГО ПРОЕКТА
**Студент:** [Твое Имя]
**Специальность:** Прикладная математика и информатика (ПМИ)
**Научный руководитель:** Елена Борисовна (требовательна к мат. базе, ценит классическую школу — Воронцов, Миркин).

---

## 1. Тема и Цель
**Полная формулировка:**
> *«Разработка электронного пособия по методам кластеризации. Разработка предполагает реализацию приложения, содержащего описания алгоритмов, примеры использования, тренажера, модуля тестирования, модуля поиска информации, базы данных, модуля подготовки информации к опубликованию. Рекомендуется использовать онтологический подход.»*

**Цель:** Создать адаптивную обучающую систему, которая не просто показывает текст, а помогает студенту подбирать алгоритмы под данные, используя семантический анализ (онтологию).

---

## 2. Математическое ядро (Алгоритмы)
**Источник теории:** Лекции К.В. Воронцова (МФТИ) и книга Б.Г. Миркина.
**Требование:** В тренажере реализация должна быть **«ручной» (на чистом Python/NumPy)**, чтобы показать понимание внутренней логики. Библиотека `scikit-learn` используется для сравнения/эталона.

**Список реализуемых алгоритмов (от базовых к сложным):**
1.  **K-Means (Метод k-средних):**
    *   *Класс:* Метрический/Центроидный.
    *   *Реализация:* Ручная (Алгоритм Ллойда).
    *   *Особенность:* Ищет сферы, чувствителен к выбросам.
2.  **ФОРЭЛЬ (FOREL):**
    *   *Класс:* Эвристический (Классика отечественной школы, есть у Миркина).
    *   *Реализация:* Ручная.
3.  **DBSCAN:**
    *   *Класс:* Плотностный.
    *   *Реализация:* Ручная (поиск соседей в радиусе $\epsilon$).
    *   *Особенность:* Находит произвольные формы, выделяет шум.
4.  **Агломеративная кластеризация (Agglomerative):**
    *   *Класс:* Иерархический.
    *   *Теория:* Описывается через **Формулу Ланса-Уильямса**.
    *   *Визуализация:* Дендрограмма.
5.  **MeanShift:** (Опционально, есть в презентации руководителя).
6.  **Спектральная кластеризация:** (Опционально, для демонстрации линейной алгебры/собственных векторов).

---

## 3. Научная новизна: Онтология
**Инструмент:** Protégé (файл `.owl`).
**Библиотека в коде:** `owlready2`.

**Функции онтологии в системе:**
1.  **«Интеллектуальный советчик» (В тренажере):**
    *   Связывает *Свойства данных* (Шум, Геометрия кластеров) с *Ограничениями алгоритмов*.
    *   *Логика:* Если данные сложные $\to$ Онтология исключает K-Means $\to$ Рекомендует DBSCAN.
2.  **«Генерация интерфейса» (Подход В.В. Грибовой):**
    *   Интерфейс тренажера (ползунки, параметры) не захардкожен, а строится на основе свойств алгоритма в онтологии.
    *   *Пример:* Если у алгоритма в онтологии есть свойство `hasParameter: Epsilon`, фронтенд сам рисует для него ползунок.
3.  **«Адаптивная траектория» (В тестах):**
    *   Связывает *Понятия* (Метрика) с *Алгоритмами*.
    *   Если студент не знает метрику, система не пускает его к изучению алгоритмов.

---

## 4. Архитектура и Технологии
**Тип:** Гибридный монолит (Hybrid Web App).

### Backend (Сервер)
*   **Стек:** **Python + Django**.
*   **Задачи:**
    *   Работа с БД (PostgreSQL/SQLite).
    *   Админка для преподавателя (загрузка лекций).
    *   API для тренажера (принимает точки, возвращает кластеры).
    *   Логика онтологии (`owlready2`).
    *   Модуль `services.py` — здесь лежат ручные реализации алгоритмов.

### Frontend (Клиент)
*   **Стек:** **Django Templates** (основные страницы) + **Vue.js** (островная архитектура).
*   **Где используется Vue.js:**
    *   **Тренажер:** Canvas для рисования точек, Plotly.js для графиков, панель с кодом алгоритма.
    *   **Тесты:** Переключение вопросов без перезагрузки.
*   **Сборка:** Без Webpack, подключение через CDN (для простоты и скорости разработки).

### Данные
*   **Синтетические:** Пользователь кликает мышкой по экрану (создает датасеты "на лету").
*   **Внешние:** Возможность загрузки CSV или парсинг статей (для модуля поиска).

---

## 5. Функциональные модули (По ТЗ)

1.  **Модуль теоретического материала:** Лекции с формулами (LaTeX).
2.  **Тренажер (Симулятор):**
    *   Интерактивный ввод данных (клики).
    *   Сплит-экран: График + Окно с кодом (чтобы студент видел, как работает алгоритм "внутри").
    *   Сравнение работы "Ручного" алгоритма и "Библиотечного".
    *   **Пошаговое отображение:** Визуализация каждой итерации алгоритма.
3.  **Модуль тестирования:** Адаптивные тесты. Ошибка возвращает к теории.
4.  **Модуль поиска:** Фасетный поиск (фильтры по свойствам алгоритмов на базе онтологии).
5.  **Личный кабинет:**
    *   **Студент:** Видит свой профиль компетенций (график успеваемости), сохраняет эксперименты.
    *   **Преподаватель:** Добавляет лекции, размечает их тегами онтологии, видит статистику группы.

---

## 6. Текущий статус разработки
1.  **Структура проекта:** Не создана.
2.  **Этап:** Инициализация. Планирование архитектуры.
3.  **Ближайшая задача:** Создание базового проекта Django и реализация прототипа тренажера (MVP).