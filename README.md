# 🧠 K-Means Interactive Trainer

**Интерактивный тренажер по алгоритмам кластеризации** для обучения Data Science.

## 📋 Возможности

- ✨ **Интерактивный симулятор** — добавляйте точки кликом, визуализируйте кластеризацию
- 📚 **Система заданий** — решайте задачи на Python прямо в браузере (Pyodide)
- 🎯 **Пошаговая визуализация** — наблюдайте, как двигаются центроиды
- 🔍 **Автопроверка решений** — мгновенная обратная связь

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Создание базы данных
```bash
python manage.py migrate
```

### 3. Наполнение заданиями
```bash
python init_tasks.py
```

### 4. Запуск сервера
```bash
python manage.py runserver
```

Откройте http://localhost:8000/simulator/

## 📁 Структура проекта

```
diploma-clustering-app/
├── apps/
│   └── simulator/          # Основное приложение
│       ├── engine.py       # Движок K-Means
│       ├── models.py       # Модель Task
│       └── views.py        # API + страницы
├── config/                 # Настройки Django
├── templates/
│   ├── base.html          # Базовый шаблон
│   └── simulator/         # Страницы приложения
├── static/
│   ├── css/               # Глобальные стили
│   └── js/                # Vue.js островки
├── db.sqlite3             # База данных
└── init_tasks.py          # Скрипт наполнения БД
```

## 🛠️ Технологии

**Backend:**
- Django 4.2 + Django REST Framework
- Scikit-learn (K-Means)
- SQLite

**Frontend (Islands Architecture):**
- Vue.js 3 (Composition API)
- Plotly.js (графики)
- Monaco Editor (редактор кода)
- Pyodide (Python в браузере)

## 📡 API Endpoints

### `POST /simulator/api/run-kmeans/`
Запуск алгоритма K-Means.

**Вход:**
```json
{
  "points": [[1.0, 2.0], [3.0, 4.0]],
  "k": 2
}
```

**Выход:**
```json
{
  "success": true,
  "history": [...],
  "total_steps": 5
}
```

### `POST /simulator/api/check-solution/`
Проверка решения задания.

**Вход:**
```json
{
  "task_id": "euclidean-dist",
  "result": 5.0
}
```

**Выход:**
```json
{
  "correct": true,
  "message": "✅ Верно!"
}
```

## 📚 Добавление новых заданий

Отредактируйте `init_tasks.py` или используйте Django Admin:

```bash
python manage.py createsuperuser
python manage.py runserver
# Перейдите на http://localhost:8000/admin/
```

## 🎓 Архитектура

Проект использует **островковую архитектуру** (Islands Architecture):
- Django рендерит HTML-страницы (SSR)
- Vue.js "оживляет" только интерактивные компоненты
- Нет полноценного SPA, только точечные островки JS

Это обеспечивает:
- ✅ Быстрый первый рендер (без JS)
- ✅ Простоту деплоя (один сервер)
- ✅ Хорошую SEO
- ✅ Прогрессивное улучшение

## 📄 Лицензия

MIT License — свободно используйте в учебных целях.

## 👨‍💻 Автор

Дипломный проект — Интерактивный тренажер по алгоритмам кластеризации.