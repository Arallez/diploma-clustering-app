# 🔗 Интеграция OWL-онтологии для адаптивного обучения

## Обзор

Система поддерживает работу с OWL-файлами онтологии кластеризации. **Основной источник** — `clustering.2.owl` (последняя версия, при его наличии). Запасные: `clustering_1.0.owl`, `clustering.owl`, `clustering_domain.owl`. Ниже описана структура на примере `clustering.owl` / `clustering_1.0.owl` - полной онтологией алгоритмов кластеризации, содержащей:

- **Алгоритмы**: K-Means, DBSCAN, BIRCH, Affinity Propagation, Agglomerative, HDBSCAN, OPTICS, Mean-shift, Spectral Clustering, Ward, Gaussian Mixtures, Bisecting K-Means
- **Метрики**: Point Distance, Nearest Point Distance, Graph Distance, Mahalanobis, Any Pairwise
- **Параметры**: number of clusters, neighborhood size, bandwidth, distance threshold, и др.
- **Характеристики**: масштабируемость, геометрия кластеров, размер кластеров, тип вывода

---

## Структура онтологии

### Классы:

- `Algorithm` - базовый класс алгоритмов
  - `PartitioningAlgorithm` - алгоритмы разбиения
  - `DensityBasedAlgorithm` - плотностные алгоритмы
  - `HierarchicalAlgorithm` - иерархические алгоритмы
  - `ModelBasedAlgorithm` - модельные алгоритмы

- `Metric` - метрики расстояния
- `Parameter` - параметры алгоритмов
- `Geometry` - геометрия кластеров
- `ClusterSize` - размер кластеров
- `Scalability` - масштабируемость
- `InferenceType` - тип вывода (Inductive/Transductive)
- `UseCase` - применения алгоритмов

### Свойства (Object Properties):

- `usesMetric` → маппится на `USES` (алгоритм использует метрику)
- `hasParameter` → маппится на `RELATED` (алгоритм имеет параметр)
- `solvesTask` → маппится на `RELATED` (алгоритм решает задачу)
- `supportsGeometry` → маппится на `RELATED` (алгоритм поддерживает геометрию)
- `assumesClusterSize` → маппится на `RELATED` (алгоритм предполагает размер кластеров)
- `hasScalability` → маппится на `RELATED` (алгоритм имеет масштабируемость)
- `hasInferenceType` → маппится на `RELATED` (алгоритм имеет тип вывода)

---

## Маппинг свойств на типы связей

| OWL Property | Relation Type | Описание |
|--------------|---------------|----------|
| `usesMetric` | `USES` | Алгоритм использует метрику (зависимость) |
| `hasParameter` | `RELATED` | Алгоритм имеет параметр |
| `solvesTask` | `RELATED` | Алгоритм решает задачу (UseCase) |
| `supportsGeometry` | `RELATED` | Алгоритм поддерживает геометрию |
| `assumesClusterSize` | `RELATED` | Алгоритм предполагает размер кластеров |
| `hasScalability` | `RELATED` | Алгоритм имеет масштабируемость |
| `hasInferenceType` | `RELATED` | Алгоритм имеет тип вывода |

**Важно**: Связи типа `USES` считаются зависимостями для адаптивного обучения. Если алгоритм использует метрику, то знание этой метрики требуется перед изучением алгоритма.

---

## Использование

### 1. Синхронизация онтологии

Рекомендуется использовать **clustering.2.owl** в корне проекта (последняя версия с полным глоссарием на русском для классов, свойств и индивидов).

```bash
python manage.py sync_ontology
```

Команда:
- Загружает `clustering.owl` (или `clustering_domain.owl` для обратной совместимости)
- Извлекает все индивиды (алгоритмы, метрики, параметры и т.д.)
- Создает Concepts в БД Django
- Создает связи между Concepts на основе свойств OWL

### 2. Примеры связей из онтологии

**K-Means (Algo_KMeans):**
- `usesMetric` → `Metric_PointDistance` (USES)
- `hasParameter` → `Param_NumClusters` (RELATED)
- `supportsGeometry` → `FlatGeometry` (RELATED)
- `assumesClusterSize` → `EvenClusterSize` (RELATED)
- `hasScalability` → `HighScalability` (RELATED)
- `hasInferenceType` → `Inductive` (RELATED)
- `solvesTask` → `UC_GeneralPurpose` (RELATED)

**DBSCAN (Algo_DBSCAN):**
- `usesMetric` → `Metric_NearestPointDistance` (USES)
- `hasParameter` → `Param_NeighborhoodSize` (RELATED)
- `supportsGeometry` → `NonFlatGeometry` (RELATED)
- `solvesTask` → `UC_OutlierRemoval` (RELATED)

### 3. Адаптивное обучение

Система автоматически определяет зависимости:

1. **Пользователь решает задачу про "Metric_PointDistance"**
   - Concept "Metric_PointDistance" помечается как изученный

2. **Система проверяет задачу про "K-Means"**
   - K-Means требует: `Metric_PointDistance` (USES)
   - Все зависимости изучены → задача доступна

3. **Система проверяет задачу про "DBSCAN"**
   - DBSCAN требует: `Metric_NearestPointDistance` (USES)
   - Если не изучено → задача заблокирована
   - Система рекомендует материал про "Metric_NearestPointDistance"

---

## Обработка URI

Файл `clustering.owl` использует полные URI:
```
http://www.semanticweb.org/diploma/clustering#Algo_KMeans
```

Система автоматически:
- Извлекает короткое имя: `Algo_KMeans`
- Сохраняет полный URI в поле `uri` модели Concept
- Использует русский `rdfs:label` для отображения названия

---

## Расширение онтологии

### Добавление нового алгоритма:

```xml
<owl:NamedIndividual rdf:about="http://www.semanticweb.org/diploma/clustering#Algo_NewAlgorithm">
  <rdf:type rdf:resource="http://www.semanticweb.org/diploma/clustering#PartitioningAlgorithm"/>
  <usesMetric rdf:resource="http://www.semanticweb.org/diploma/clustering#Metric_PointDistance"/>
  <hasParameter rdf:resource="http://www.semanticweb.org/diploma/clustering#Param_NumClusters"/>
  <rdfs:label xml:lang="ru">Новый алгоритм</rdfs:label>
</owl:NamedIndividual>
```

После добавления выполните `python manage.py sync_ontology`.

---

## Отличия от clustering_domain.owl

| Аспект | clustering_domain.owl | clustering.owl |
|--------|----------------------|----------------|
| Размер | ~70 строк, 5 индивидов | ~626 строк, 50+ индивидов |
| Namespace | Локальный файл | `http://www.semanticweb.org/diploma/clustering#` |
| Свойства | `uses_metric`, `requires_knowledge` | `usesMetric`, `hasParameter`, `solvesTask`, и др. |
| Структура | Простая | Иерархическая (классы алгоритмов) |
| Применение | Базовые примеры | Полная онтология для диплома |

Система поддерживает оба файла для обратной совместимости.
