# 🎓 Образовательная платформа: Алгоритмы Кластеризации

Интерактивная веб-платформа для изучения и визуализации алгоритмов машинного обучения (кластеризации). Проект разработан в рамках дипломной работы.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Python](https://img.shields.io/badge/python-3.10%2B-blue)
![Django](https://img.shields.io/badge/django-5.0-green)
![Vue.js](https://img.shields.io/badge/vue.js-3.0-green)

---

## 🏗 Архитектура проекта

Проект построен на архитектуре **Hybrid Monolith** (Гибридный Монолит), объединяющей надежность серверного рендеринга и интерактивность SPA.

### 🧩 Компоненты системы

1.  **Backend (Django 5)**
    *   **Core (MVT):** Отвечает за маршрутизацию, авторизацию и статические страницы.
    *   **Algorithm Engine (`algorithms.py`):** "Чистое" математическое ядро на базе `NumPy`. Не зависит от веб-фреймворка. Реализует логику кластеризации (K-Means, DBSCAN, FOREL, etc.).
    *   **Execution Sandbox (`services.py`):** Сервисный слой безопасности. Отвечает за статический анализ (AST) и безопасное исполнение пользовательского кода.
    *   **API Layer:** REST-подобные эндпоинты для взаимодействия с симулятором.

2.  **Frontend (Vue.js 3 + Plotly.js)**
    *   **Embedded Vue:** Vue.js встраивается "островами" в Django-шаблоны. Это позволяет избежать сложности SPA (CORS, JWT), сохраняя реактивность интерфейса.
    *   **Visualization:** Plotly.js используется для рендеринга интерактивных графиков (Scatter plots, Dendrograms) в реальном времени.

---

## 🛡️ Безопасность и Sandbox

Особое внимание уделено безопасности выполнения кода, так как платформа позволяет студентам писать решения на Python.

### 1. Статический анализ кода (AST)
Перед запуском любой пользовательский код проходит проверку через модуль `ast`:
*   ❌ Запрещены опасные импорты (`os`, `sys`, `subprocess`, `shutil`).
*   ❌ Запрещены опасные функции (`exec`, `eval`, `open`, `__import__`).
*   ❌ Запрещен доступ к приватным атрибутам (начинающимся с `_`).
*   ✅ Разрешен "Белый список" (`math`, `random`, `numpy`, `scipy`, `sklearn`).

### 2. Защита от бесконечных циклов (Trace Limit)
Используется кастомный `sys.settrace` трейсер:
*   Счетчик инструкций отслеживает выполнение каждой строки кода.
*   Лимит установлен на **200,000 операций**.
*   При превышении выбрасывается `TimeLimitException`, предотвращая DoS-атаку (зависание сервера).

### 3. Гибридная CSRF-защита
*   **Simulator API:** Открыт (`@csrf_exempt`) для обеспечения быстродействия и совместимости с разными браузерами в режиме "Гостя". Безопасно, так как этот API не изменяет состояние БД.
*   **Task/Solution API:** Строго защищен (`CsrfViewMiddleware`). Требует валидный токен для выполнения проверки решений.

---

## 🚀 Функциональность

### 1. Интерактивный Симулятор
Песочница для визуального изучения алгоритмов без написания кода.
*   **Поддерживаемые алгоритмы:**
    *   🟢 **K-Means:** Классический центроидный алгоритм.
    *   🔵 **DBSCAN:** Плотностной алгоритм (кластеризация произвольных форм).
    *   🔴 **FOREL:** Эвристический алгоритм (гиперсфера).
    *   🟣 **Иерархическая (Agglomerative):** Построение дендрограмм.
    *   🟠 **MeanShift:** Сдвиг среднего (поиск мод плотности).
*   **Управление:** Пошаговая навигация ("плеер" истории шагов), настройка параметров (K, Epsilon, Radius) на лету.
*   **Датасеты:** Генерация синтетических данных (Moons, Blobs, Circles) и ручное добавление точек кликом.

### 2. Система Задач (LMS)
Образовательный модуль для проверки знаний.
*   **Теоретические тесты:** Вопросы с множественным выбором.
*   **Задачи на кодинг:** Встроенный редактор (Ace Editor). Студент должен реализовать функцию (например, `euclidean_distance`), которая проверяется юнит-тестами на сервере.

---

## 📂 Структура проекта

```text
diploma-clustering-app/
├── apps/
│   ├── core/               # Главная страница, профили
│   ├── simulator/          # Основное приложение
│   │   ├── algorithms.py   # 🧠 Математическое ядро (NumPy)
│   │   ├── services.py     # 🛡️ Слой безопасности (AST, Sandbox)
│   │   ├── views.py        # 🎮 Контроллеры (Clean Controllers)
│   │   └── urls.py         # Роутинг
│   └── users/              # Авторизация и регистрация
├── static/
│   ├── js/
│   │   ├── simulator/      # Vue.js приложение
│   │   │   ├── app.js      # Логика состояния
│   │   │   ├── api.js      # Клиент API (c CSRF защитой)
│   │   │   └── plot.js     # Отрисовка Plotly
│   └── css/
├── templates/              # HTML Шаблоны (Django Jinja2)
└── manage.py
```

---

## 🛠 Установка и запуск

### Требования
*   Python 3.10+
*   PostgreSQL (опционально, по умолчанию SQLite)

### Шаги
1.  **Клонировать репозиторий:**
    ```bash
    git clone https://github.com/Arallez/diploma-clustering-app.git
    cd diploma-clustering-app
    ```

2.  **Создать виртуальное окружение:**
    ```bash
    python -m venv venv
    # Windows:
    .\venv\Scripts\activate
    # Linux/Mac:
    source venv/bin/activate
    ```

3.  **Установить зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Применить миграции:**
    ```bash
    python manage.py migrate
    ```

5.  **Запустить сервер:**
    ```bash
    python manage.py runserver
    ```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`

---

## 👨‍💻 Автор
Проект разработан в рамках дипломной работы.
**Стек:** Python, Django, NumPy, Vue.js, Plotly.
