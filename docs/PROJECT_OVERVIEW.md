## 🎓 Обзор проекта: платформа для изучения кластеризации

Этот документ описывает **текущее состояние проекта** на высоком уровне: назначение, архитектуру, приложения, ключевую функциональность, API, безопасность и возможные направления развития.

Старые документы (`README.md`, `docs/ARCHITECTURE.md`, `docs/API.md`) **не изменялись** и могут использоваться как историческая справка.

---

## 🧠 Назначение проекта

- **Тип проекта**: образовательная платформа по теме кластеризации и анализа данных.
- **Цели**:
  - Интерактивное изучение алгоритмов кластеризации.
  - Решение задач (как в онлайн-тренажёрах / LMS).
  - Изучение теоретических понятий через онтологию и граф связей.
- **Основные компоненты с точки зрения пользователя**:
  - Личный кабинет и профиль.
  - Теоретические материалы.
  - Интерактивный симулятор кластеризации.
  - Система задач (кодинг и тесты).
  - Энциклопедия/онтология понятий.

---

## 🏗 Общая архитектура

Проект реализован как **Django-монолит** с серверным рендерингом (Django templates) и **«островками» Vue.js** там, где нужна высокая интерактивность (симулятор, задачи).

- **Backend**:
  - Django (MVT).
  - Численные библиотеки: `numpy`, `scipy`, `scikit-learn`.
  - Работа с онтологиями: `owlready2`.
  - Безопасное выполнение кода: `ast`, `func-timeout`, собственный sandbox.
- **Frontend**:
  - Django-шаблоны (HTML + Jinja-подобный синтаксис).
  - Vue.js 3 (подключается через статику / vendor).
  - Plotly.js для визуализации данных и истории работы алгоритмов.
- **База данных**:
  - По умолчанию `SQLite` (через стандартный `django.db.backends.sqlite3`).

---

## 📂 Структура проекта (актуальная)

Упрощённая структура:

```text
diploma-clustering-app/
├── apps/
│   ├── core/               # Главная, аутентификация, профиль, материалы
│   ├── simulator/          # Симулятор кластеризации и задачи
│   └── encyclopedia/       # Онтология понятий и связи
├── config/                 # Настройки и корневой роутинг Django
├── static/                 # Статические файлы (CSS, JS, vendor-библиотеки)
├── templates/              # HTML-шаблоны
├── docs/                   # Документация (этот файл, ARCHITECTURE, API)
├── clustering_domain.owl   # OWL-онтология домена кластеризации
├── manage.py
└── requirements.txt
```

---

## 🧩 Django‑приложения

### 1. `apps/core`

**Назначение**: базовая оболочка платформы.

- **Функциональность**:
  - Главная страница.
  - Регистрация и аутентификация пользователя.
  - Личный профиль (прогресс, статистика по задачам).
  - Просмотр учебных материалов (лекции/статьи).
- **Ключевые сущности**:
  - `Material`:
    - Заголовок, slug, содержимое, порядок отображения.
    - Связь с тегами задач (`TaskTag` из `simulator`) через M2M — позволяет привязывать теорию к практическим задачам.
- **Особенности**:
  - При регистрации пользователя может отправляться приветственное письмо.
  - Профиль агрегирует результаты попыток по задачам из `simulator`.

### 2. `apps/simulator`

**Назначение**: ядро учебного процесса — симулятор кластеризации и система задач.

- **Модели**:
  - `TaskTag` — теги/темы задач (например, «K-Means», «Метрики расстояния»).
  - `Task` — описание задач:
    - тип (код / тест),
    - сложность,
    - входные и ожидаемые данные (`test_input`, `expected_output`),
    - имя требуемой функции (`function_name`),
    - текст условия и пояснения.
  - `UserTaskAttempt` — попытки решения пользователем:
    - ссылка на пользователя и задачу,
    - статус (успешно/ошибка),
    - сохранённый код / ответ,
    - временные метки.
- **Ключевые файлы**:
  - `algorithms.py`:
    - Реализации алгоритмов кластеризации:
      - **K-Means**
      - **DBSCAN**
      - **FOREL**
      - **Иерархическая кластеризация (Agglomerative)**
      - **MeanShift**
    - Обработка и нормализация входных данных (`normalize_points`).
    - Возврат **истории шагов** для анимации на фронтенде (центроиды, метки, расстояния и т.п.).
  - `presets.py`:
    - Наборы синтетических датасетов:
      - blobs, moons, circles,
      - «решётка», «иерархическая структура», «плотные/редкие кластеры» и т.д.
  - `services.py`:
    - Sandbox для безопасного запуска пользовательского кода.
    - AST-проверка, ограничения на импорты и использование опасных функций.
    - Лимит по количеству исполняемых инструкций/по времени.
  - `views.py` и `urls.py`:
    - Веб‑страницы симулятора и задач.
    - JSON‑эндпоинты для фронтенда.

### 3. `apps/encyclopedia`

**Назначение**: энциклопедия понятий и их связей на основе OWL‑онтологии.

- **Модели**:
  - `Concept` — концепт/понятие:
    - URI, название, описание.
  - `ConceptRelation` — связь между понятиями:
    - `source`, `target`,
    - `relation_type` (например: `IS_A`, `PART_OF`, `USES`, `DEPENDS`, `RELATED`).
- **Логика работы**:
  - `ontology.py`:
    - Загрузка OWL‑файла `clustering_domain.owl` через `owlready2`.
    - Сопоставление классов/индивидов объектам `Concept`.
    - Создание связей `ConceptRelation` на основе объектных и дата‑свойств онтологии.
  - Views:
    - Страница с графом понятий.
    - Детальная страница конкретного концепта с его связями.

---

## 🌐 Конфигурация и роутинг

- **Корневые настройки**: `config/settings.py`
  - Подключение `apps.core`, `apps.simulator`, `apps.encyclopedia`.
  - Настройки базы данных (по умолчанию SQLite).
  - Подключение статики и шаблонов.
- **Корневой роутер**: `config/urls.py`
  - Основные маршруты:
    - `/admin/` — админ-панель Django.
    - `/` — главная/домашняя страница.
    - `/simulator/` — раздел симулятора и задач.
    - `/encyclopedia/` — раздел энциклопедии.
    - Пути для регистрации/логина/логаута.
- **WSGI/ASGI**:
  - `config/wsgi.py` — точка входа для Gunicorn и подобных серверов.
  - `config/asgi.py` — готовность к асинхронным сценариям (при необходимости).

---

## 📊 Симулятор кластеризации

### Фронтенд

- Vue.js‑компонент симулятора встраивается в шаблон (Island).
- Plotly.js:
  - Отрисовка точек, кластеров, центроидов.
  - Пошаговая анимация выполнения алгоритма (переключение по истории).
- Пользователь может:
  - выбрать предустановленный датасет (через пресеты),
  - добавить точки вручную (клики по плоскости),
  - выбрать алгоритм и параметры (k, ε, minPts, радиус, bandwidth и др.),
  - запускать и перезапускать симуляцию, смотреть историю шагов.

### Backend‑API симулятора

Используются **обычные Django‑views** с `JsonResponse` (DRF подключён в зависимостях, но сейчас не используется в качестве основного слоя API).

Типичные эндпоинты:

- `GET /simulator/preset/`
  - Генерация наборов данных (blobs, moons, circles, и др. варианты).
  - Возвращает точки и метаданные.
- `POST /simulator/run/`
  - Запуск выбранного алгоритма кластеризации.
  - Ожидает JSON c:
    - `algorithm`: строка (`"kmeans"`, `"dbscan"`, `"forel"`, `"agglomerative"`, `"meanshift"`),
    - `points`: массив точек,
    - `params`: объект с параметрами алгоритма.
  - Возвращает:
    - `success`: флаг,
    - `history`: подробный список шагов для анимации.
- `POST /simulator/dendrogram/`
  - Специальный эндпоинт для построения дендрограммы (иерархическая кластеризация).

Часть эндпоинтов симулятора помечена `@csrf_exempt`, так как:

- они **не изменяют состояние БД**,
- используются только для вычислений и визуализации.

---

## 🧪 Система задач (LMS‑часть)

- **Типы задач**:
  - Теоретические (выбор вариантов ответа).
  - Практические (написание кода на Python).
- **Поток для пользователя**:
  1. Выбор задачи в списке (по тегам/темам).
  2. Переход на детальную страницу задачи.
  3. Ввод решения (код или ответ).
  4. Отправка решения на сервер.
  5. Получение результата проверки (успех/ошибка, сообщения, возможно — подсказки).

### Backend‑логика проверок

- Отдельный сервис‑слой (`services.py`) для:
  - Статического анализа кода:
    - AST‑парсинг,
    - запрет опасных модулей (`os`, `sys`, `subprocess`, и т.д.),
    - запрет опасных функций (`exec`, `eval`, `open`, `__import__`, и т.п.),
    - запрет приватных/магических атрибутов.
  - Ограничения на ресурсы:
    - лимит количества выполняемых инструкций/строк,
    - таймаут исполнения (через `func-timeout`).
  - Сравнение результатов:
    - запуск пользовательской функции на тестовых данных,
    - сравнение с ожидаемым выводом.
- История попыток сохраняется в `UserTaskAttempt`.

Эндпоинт проверки решений (названия могут отличаться в реализации, но концептуально):

- `POST /simulator/api/check-solution/`
  - Принимает:
    - идентификатор задачи,
    - код/ответ пользователя.
  - Возвращает:
    - флаг корректности,
    - сообщение/обратную связь.
  - Под защитой CSRF, так как работает с пользовательскими данными и БД.

---

## 📚 Энциклопедия и онтология

### OWL‑онтология `clustering_domain.owl`

- Описывает домен кластеризации:
  - классы: алгоритмы, метрики, знания, объекты и др.
  - индивиды: конкретные алгоритмы (KMeans, DBSCAN), метрики (EuclideanDistance и т.п.).
  - отношения: «использует метрику», «требует знания», «часть чего‑то» и т.д.

### Интеграция с Django

- При помощи `owlready2`:
  - OWL‑онтология грузится в Python‑объекты.
  - На основе онтологии формируются:
    - объекты `Concept`,
    - связи `ConceptRelation` (с типами `IS_A`, `PART_OF`, `USES`, `DEPENDS`, `RELATED` и др.).
- На фронтенде:
  - страница с графом понятий,
  - страница отдельного понятия с его описанием и связями.

---

## 🔐 Безопасность

Основные меры безопасности:

- **Sandbox для выполнения кода**:
  - AST‑анализ и чёрный список опасных операций.
  - Белый список разрешённых модулей (математика, NumPy, и т.п.).
  - Ограничения по времени и количеству инструкций.
- **CSRF‑защита**:
  - Включена глобально для Django.
  - Снята (`@csrf_exempt`) только там, где:
    - нет изменения состояния БД,
    - только чтение/вычисления (симулятор).
  - Для проверки решений задач CSRF‑защита включена.
- **Входные данные**:
  - Валидация форм и JSON до передачи в алгоритмы.
- **Потенциальные точки улучшения**:
  - Добавить rate limiting (например, `django-ratelimit` или ограничение на уровне прокси/Nginx).
  - Жёстче контролировать размер входных данных для симулятора.

---

## 🧪 Тесты, CI/CD, Docker

На текущем этапе:

- **Unit/Integration‑тесты**:
  - В проекте отсутствуют (`tests.py` или `test_*.py` не обнаружены).
  - Логичный шаг развития — покрыть тестами:
    - алгоритмы в `algorithms.py`,
    - sandbox‑логику,
    - ключевые view/эндпоинты.
- **CI/CD**:
  - Нет конфигов для GitHub Actions / GitLab CI / и т.п.
  - Возможное улучшение — добавить:
    - прогон тестов при каждом пуше,
    - линтер (flake8/ruff, isort, mypy при необходимости).
- **Docker/Compose**:
  - В репозитории нет Dockerfile и docker‑compose.
  - Для продакшена / развёртывания в разных окружениях Docker сильно упростит жизнь.

---

## 🧰 Технологии (актуальный стек)

- **Backend**:
  - Python 3.10+
  - Django (4.2–5.x)
  - NumPy, SciPy, scikit-learn
  - owlready2
  - func-timeout
- **Frontend**:
  - Vue.js 3 (через статику/vendor)
  - Plotly.js
  - Классические HTML‑шаблоны Django
- **Инфраструктура**:
  - SQLite (по умолчанию)
  - Gunicorn/Whitenoise (ориентация на деплой через WSGI)

В `requirements.txt` также могут присутствовать:

- `djangorestframework` — пока используется ограниченно или не используется для основных эндпоинтов.
- `django-cors-headers` — может быть добавлен, но в текущей конфигурации CORS, возможно, минимален.

---

## 🚧 Известные несоответствия и направления развития

Ниже — **краткий список того, что можно улучшить** в текущей версии:

- **Документация**:
  - Ранее написанные `README.md`, `docs/ARCHITECTURE.md`, `docs/API.md` местами описывают другую архитектуру (например, упор на DRF и Pyodide).
  - Этот файл (`docs/PROJECT_OVERVIEW.md`) отражает **более актуальную картину**, но старые документы сохранены для истории.
- **API‑слой**:
  - DRF подключён в зависимостях, но большая часть API реализована как обычные Django‑views.
  - Возможное улучшение — перевести ключевые JSON‑эндпоинты на DRF (viewsets/serializers).
- **Мелкие несоответствия в коде**:
  - Возможны неточности namespace'ов в `reverse()` (например, в `Material.get_absolute_url`).
  - Требуется периодический рефакторинг URL‑имен, чтобы соответствовать фактической структуре.
- **Безопасность и продакшен‑настройки**:
  - `ALLOWED_HOSTS` в dev‑режиме может быть пустым — для продакшена нужно настроить.
  - Необходимо вынести чувствительные настройки (SECRET_KEY, БД и т.д.) в `.env`.
- **Производительность и масштабирование**:
  - SQLite подходит для разработки и малых нагрузок, но не для высоконагруженного продакшена.
  - Scikit-learn и NumPy выполняются синхронно — при больших датасетах возможны блокировки.

---

## ✅ Резюме

Проект представляет собой **современную учебную платформу по кластеризации**, сочетающую:

- интерактивную визуализацию алгоритмов,
- систему задач с безопасным выполнением пользовательского кода,
- онтологию понятий и граф знаний.

Архитура понятная и расширяемая: Django‑монолит + Vue/Plotly‑островки + OWL‑онтология. Основные следующие шаги развития — тесты, CI/CD, Docker, выравнивание старой документации с фактическим кодом и, при желании, постепенный переход к более формальному API‑слою на DRF.

