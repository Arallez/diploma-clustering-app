# 🎯 План интеграции OWL-онтологии для адаптивного обучения

## Текущее состояние

- ✅ Есть OWL файл `clustering_domain.owl` с онтологией
- ✅ Есть модели `Concept` и `ConceptRelation` в БД
- ✅ Есть функция `sync_ontology()` но она создает онтологию, а не загружает существующий файл
- ✅ Есть связи между Material ↔ TaskTag ↔ Task
- ❌ Нет связи между Task/Material и Concept из онтологии
- ❌ Нет системы рекомендаций на основе зависимостей

---

## Шаг 1: Загрузка существующего OWL файла

### Изменить `apps/encyclopedia/ontology.py`

Текущая функция `sync_ontology()` создает онтологию программно. Нужно изменить её, чтобы она:
1. Загружала существующий `clustering_domain.owl` файл
2. Парсила его и синхронизировала с БД Django

---

## Шаг 2: Связь Task/Material с Concept

### Добавить поля в модели:

**Task:**
- `concept` - ForeignKey к Concept (опционально)
- Связь: какая задача связана с каким понятием из онтологии

**Material:**
- `concept` - ForeignKey к Concept (опционально)
- Связь: какой материал объясняет какое понятие

---

## Шаг 3: Система рекомендаций на основе зависимостей

### Логика адаптивного обучения:

1. **Определение текущего уровня знаний:**
   - Какие задачи решены пользователем (UserTaskAttempt с is_correct=True)
   - Какие Concepts связаны с решенными задачами

2. **Поиск зависимостей:**
   - Для каждой нерешенной задачи найти связанный Concept
   - Проверить через онтологию: какие Concepts требуются (requires_knowledge)
   - Проверить, изучены ли все зависимости

3. **Рекомендации:**
   - Показывать только те задачи, у которых все зависимости изучены
   - Рекомендовать материалы для изучения недостающих Concepts
   - Показывать прогресс по зависимостям

---

## Шаг 4: API для рекомендаций

### Создать сервис `apps/encyclopedia/recommendations.py`:

```python
def get_recommended_tasks(user):
    """Возвращает задачи, которые пользователь может решить (все зависимости изучены)"""
    
def get_recommended_materials(user):
    """Возвращает материалы для изучения недостающих Concepts"""
    
def get_learning_path(user, target_concept):
    """Возвращает путь обучения от текущего уровня до target_concept"""
```

---

## Шаг 5: Интеграция в UI

### Обновить views:

- **Профиль пользователя:** показать рекомендуемые задачи и материалы
- **Список задач:** фильтровать по доступности (зависимости изучены)
- **Детальная страница задачи:** показать требуемые Concepts и ссылки на материалы

---

## Пример использования:

1. Пользователь решает задачу "Евклидово расстояние" → Concept "EuclideanDistance" помечается как изученный
2. Система проверяет: для задачи "K-Means" требуется Concept "EuclideanDistance" и "Centroid"
3. Если "Centroid" не изучен → система рекомендует материал про центроид
4. После изучения всех зависимостей → задача "K-Means" становится доступной

---

## Файлы для изменения:

1. `apps/encyclopedia/ontology.py` - загрузка OWL файла
2. `apps/tasks/models.py` - добавить поле `concept`
3. `apps/core/models.py` - добавить поле `concept` к Material
4. `apps/encyclopedia/recommendations.py` - новый файл с логикой рекомендаций
5. `apps/core/views.py` - обновить profile для показа рекомендаций
6. `apps/tasks/views.py` - фильтрация задач по доступности
